[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "cjindex"
version = "0.1.0"
description = "Python bindings for cjindex"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
