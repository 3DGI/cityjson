#!/usr/bin/env just --justfile

_default:
    just --list

# Build the shared FFI core crate.
ffi-build:
    cargo build --manifest-path ffi/core/Cargo.toml --release --lib

# Run the shared FFI core crate tests.
ffi-test:
    cargo test --manifest-path ffi/core/Cargo.toml --all-features

# Run the shared FFI validation pipeline.
ffi:
    just ffi-build
    just ffi-test

# Fast compile-time validation for library, binary, and test targets.
check:
    cargo check -p cityjson-index --lib --bin cjindex --tests

# Build the production binary.
build *args:
    cargo build -p cityjson-index --bin cjindex {{args}}

clean:
    cargo clean
    rm -rf ffi/python/dist ffi/python/build
    

# Run clippy with the project's strict lint settings.
lint:
    cargo clippy -p cityjson-index --lib --bin cjindex --tests -- -Dclippy::all -Dclippy::pedantic

# Format this workspace with rustfmt without traversing sibling path dependencies.
fmt:
    cargo fmt -p cityjson-index -p cityjson-index-ffi-core

# Check rustfmt without traversing sibling path dependencies.
fmt-check:
    cargo fmt -p cityjson-index -p cityjson-index-ffi-core -- --check

# Run the library and integration tests.
test:
    cargo test -p cityjson-index --lib --tests

# Build the shared library and run the Python install smoke tests through tox and tox-uv.
test-python:
    cargo build --manifest-path ffi/core/Cargo.toml --release --lib
    sh -c 'set -eu; tmpdir=$(mktemp -d); sh scripts/build-cityjson-lib-python-wheel.sh "$tmpdir"; cd ffi/python && UV_FIND_LINKS="$tmpdir" uv run tox run'

# Build API docs for all features without pulling in dependencies.
doc:
    cargo doc --all-features --no-deps

# Build the Python bindings wheel.
build-python:
    uv build ffi/python/ --wheel --out-dir ffi/python/dist

# Run the release-quality validation pipeline.
ci:
    just fmt-check
    just check
    just lint
    just test
    just ffi
    just test-python
    just doc
