requires = ["tox>=4", "tox-uv>=1"]
env_list = ["wheel"]

[env_run_base]
description = "run the Python smoke tests against an installed package"
pass_env = ["RUSTFLAGS"]
commands = [["python", "-m", "unittest", "tests.test_api"]]

[env.wheel]
package = "wheel"
