[build-system]
requires = ["setuptools>=69", "wheel>=0.43"]
build-backend = "setuptools.build_meta"

[project]
name = "cjlib"
version = "0.3.0"
description = "Python bindings for cjlib"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
